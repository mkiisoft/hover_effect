name: example
description: Hover - Tilt 3D Effect Demo.

version: 1.0.0+2

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  hover_effect:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true